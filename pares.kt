fun main() {
    // Creamos un par de enteros que representa un punto en un plano
    val punto = Pair(3, 5)
    // Obtenemos la coordenada x del punto
    val x = punto.first
    // Imprimimos la coordenada x del punto
    println("La coordenada x del punto es $x.")
}